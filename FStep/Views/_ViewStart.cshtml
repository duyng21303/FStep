@{
    if (User.IsInRole("Admin"))
    {
        Layout = "_LayoutAdmin";
    }
    // else if (User.IsInRole("Moderator")){
    //     Layout = "_LayoutModer";
    //     System.Diagnostics.Debug.WriteLine("moder"); // Ghi thông điệp debug
    // }
    else
    {
        Layout = "_LayoutCustomer";
        System.Diagnostics.Debug.WriteLine("customer"); // Ghi thông điệp debug

    }
}
